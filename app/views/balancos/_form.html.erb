<%= form_for(@balanco) do |f| %>
  <% if @balanco.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@balanco.errors.count, "error") %> prohibited this localizacao from being saved:</h2>

      <ul>
      <% @balanco.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
