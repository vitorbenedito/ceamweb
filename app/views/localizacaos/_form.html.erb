<%= form_for(@localizacao) do |f| %>
  <% if @localizacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@localizacao.errors.count, "error") %> prohibited this localizacao from being saved:</h2>

      <ul>
      <% @localizacao.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br />
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
